version: "3"
services:
  task-lerenew:
    logging: { driver: "awslogs", options: { awslogs-create-group: "true", awslogs-group: "${AWSLOGS_GROUP}", awslogs-stream: "run-task" } }
  task-leissue:
    logging: { driver: "awslogs", options: { awslogs-create-group: "true", awslogs-group: "${AWSLOGS_GROUP}", awslogs-stream: "run-task" } }
  task-openssl:
    logging: { driver: "awslogs", options: { awslogs-create-group: "true", awslogs-group: "${AWSLOGS_GROUP}", awslogs-stream: "run-task" } }
  task-wp:
    logging: { driver: "awslogs", options: { awslogs-create-group: "true", awslogs-group: "${AWSLOGS_GROUP}", awslogs-stream: "run-task" } }
